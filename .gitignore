# Credenciais — o token NUNCA entra no versionamento
.env
.env.*
!.env.example
*.token
config.local.*

# Binários e artefatos de build
*.exe
*.exe~
*.dll
*.so
*.dylib
/build/bin/
/gommit
/gommit.exe

# Testes
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Frontend (Wails)
node_modules/

# Bindings gerados pelo `wails build`/`wails dev` a partir dos metodos ligados
# em app.go. Sao derivados do codigo Go, entao versiona-los so criaria conflito
# e a chance de o commitado divergir do gerado.
frontend/wailsjs/

# IDE / SO
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
